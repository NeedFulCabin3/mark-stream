# mark-stream
An in-browser plain text formatter that converts syntax into structured markup instantly using a custom regex engine. Built with standard web technologies, featuring direct DOM updates and side-by-side editing panes without external build steps or heavy node_modules folders.
